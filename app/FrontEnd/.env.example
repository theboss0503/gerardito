# URL base de la API de Gerardito.
# En desarrollo con el proxy de Vite, deja el valor por defecto "/api/v1".
# Si la API está en otro host, cámbiala, ej: http://localhost:8000/api/v1
VITE_API_URL=/api/v1
